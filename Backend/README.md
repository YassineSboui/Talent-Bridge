# Backend

Main API layer.

The backend should orchestrate modules but should not contain heavy business logic directly.

Current API project:

- `TalentBridgeAPI`

The project still includes compatibility wrappers during migration.
