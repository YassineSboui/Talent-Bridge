# Core NLP
spacy>=3.5.0,<4.0.0
spacy-transformers>=1.2.0
torch>=2.0.0

# API
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6

# PDF extraction
pymupdf>=1.23.0

# Multilingual support
deep-translator>=1.11.0
langdetect>=1.0.9

# Data processing
scikit-learn>=1.3.0
numpy>=1.24.0

# SQL Server job matching
pyodbc>=5.0.0
SQLAlchemy>=2.0.0

# Optional stronger semantic embeddings. TF-IDF remains the default fallback.
# Keep this on the 2.x line for compatibility with the current transformers stack.
sentence-transformers>=2.6.1,<3.0.0

# Optional advanced clustering. K-Means remains the default fallback.
hdbscan>=0.8.33; platform_system != "Windows"

# Test runner
pytest>=8.0.0
