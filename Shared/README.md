# Shared

Reserved for shared utilities, schemas, configuration, validation, and database helpers.

This folder currently contains documentation only. Add shared code here only when at least two domains need the same implementation.

Dependency rule: shared code must not import backend route code or domain-specific implementations.
